import {describe,expect,test} from "bun:test";import {projectEvidence} from "./evidence-projector";
describe("evidence projector",()=>{test("creates factual claims with excerpts",()=>{const claims=projectEvidence("Experience\nSenior Backend Engineer at Acme\nBuilt payment platform with PostgreSQL and Kafka\nB.Tech, Example University");expect(claims.some(v=>v.claimType==="skill"&&v.value==="PostgreSQL")).toBe(true);expect(claims.every(v=>v.excerpt.length>0)).toBe(true)});test("does not project protected attributes",()=>expect(projectEvidence("Gender: Female Religion: X")).toEqual([]))});
