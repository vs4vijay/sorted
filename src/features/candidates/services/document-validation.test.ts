import { describe,expect,test } from 'bun:test';import { validateCandidateDocument } from './document-validation';
describe('candidate document validation',()=>{test('accepts a PDF only when its signature matches',()=>{const file=new File([new Uint8Array([0x25,0x50,0x44,0x46,0x2d])],'synthetic.pdf',{type:'application/pdf'});expect(validateCandidateDocument(file,new Uint8Array([0x25,0x50,0x44,0x46,0x2d])).mediaType).toBe('application/pdf')});test('rejects renamed content',()=>{const file=new File(['not a pdf'],'synthetic.pdf');expect(()=>validateCandidateDocument(file,new TextEncoder().encode('not a pdf'))).toThrow('signature')});test('rejects unsupported formats',()=>{const file=new File(['candidate'],'candidate.txt');expect(()=>validateCandidateDocument(file,new TextEncoder().encode('candidate'))).toThrow('only PDF and DOCX')})});
