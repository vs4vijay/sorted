import { requireCurrentAccess } from '@/lib/auth/session';
import { CandidateIngestionRepository } from '@/features/candidates/repositories/candidate-ingestion-repository';
import { privateDocumentStorage } from '@/features/candidates/services/private-document-storage';
export const runtime='nodejs';
export async function GET(request:Request,context:RouteContext<'/api/candidate-documents/[id]'>){const access=await requireCurrentAccess();const{id}=await context.params;const row=await new CandidateIngestionRepository().getDocument(access.organization.id,id);if(!row)return new Response('Not found',{status:404});const url=new URL(request.url);const expires=Number(url.searchParams.get('expires'));const signature=url.searchParams.get('signature')??'';if(!privateDocumentStorage.verify(String(row.storage_key),expires,signature))return new Response('Link expired',{status:403});const bytes=await privateDocumentStorage.get(String(row.storage_key));return new Response(bytes,{headers:{'content-type':String(row.media_type),'content-disposition':`inline; filename="candidate-document.${String(row.media_type)==='application/pdf'?'pdf':'docx'}"`,'cache-control':'private, no-store'}})}
